import React from "react";

/**
 * Toggle method does not take any parameter
 */

const Toggle = () => {
    return (
        <div>
            <button>one</button>
            <button>two</button>
            <button>three</button>
        </div>
    );
};

export default Toggle;
